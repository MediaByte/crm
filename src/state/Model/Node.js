/**
 * @typedef {import('./PropDeclaration').PropDeclaration} PropDeclaration
 */

/**
 * Node declaration, groups a name of a node and the properties its data entries
 * will hold.
 * @typedef {object} NodeDeclaration
 * @prop {string} name User defined name.
 * @prop {PropDeclaration[]} props Prop declarations.
 */
